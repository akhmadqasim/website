---
paths:
  - "components/**/*.tsx"
---

# Component Rules

- Use `motion/react` for animations (not `framer-motion`)
- Client components must have `"use client"` directive at top
- shadcn components live in `components/ui/`
- Section components live in `components/sections/`
- Follow Maia preset style: rounded corners, generous spacing
